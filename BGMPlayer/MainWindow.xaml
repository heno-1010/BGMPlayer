<Window x:Class="BGMPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:BGMPlayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="464" Width="862">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,5">
            <Button x:Name="ButtonGo" DockPanel.Dock="Right" Click="ButtonGo_Click" Content="Go"/>
            <TextBox x:Name="addressBar" Width="Auto" Cursor="IBeam" Margin="0,0,5,0"/>
        </DockPanel>
        <wv2:WebView2 Grid.Row="1" Grid.Column="0" Name="webView" Source="https://www.youtube.com" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

        <DockPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,5">
            <TextBlock Text="Playlist" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <StackPanel DockPanel.Dock="Bottom" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Content="+" FontSize="16" Margin="0,0,5,0" MaxHeight="30"/>
                <Button Content="-" FontSize="16" MaxHeight="30"/>
            </StackPanel>
        </DockPanel>
        <ListBox Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Name="playlistBox" VerticalAlignment="Stretch" SelectionChanged="playlistBox_SelectionChanged">
        </ListBox>
    </Grid>
</Window>